<form method="post" action="{{ path('app_organism_admin_delete', {'id': organism_admin.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ organism_admin.id) }}">
    <button class="btn btn-outline-danger m-1">Delete</button>
</form>
