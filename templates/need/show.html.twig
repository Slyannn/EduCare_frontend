{% extends 'base.html.twig' %}

{% block title %}Need{% endblock %}

{% block body %}
    <h1>Need</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ need.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ need.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_need_index') }}">back to list</a>

    <a href="{{ path('app_need_edit', {'id': need.id}) }}">edit</a>

    {{ include('need/_delete_form.html.twig') }}
{% endblock %}
